version: "3"

services:
  mkdocs:
    container_name: mkdocs
    build: ./Pinting-Docs
    pull_policy: never
    volumes:
      - ./:/home
    working_dir: /home/Pinting-Docs
    ports:
      - 8888:8888
    command: sh run.sh
